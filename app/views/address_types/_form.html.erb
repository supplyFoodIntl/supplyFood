<%= form_with(model: address_type, local: true,class: "form-horizontal") do |form| %>
  <% if address_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(address_type.errors.count, "error") %> prohibited this address_type from being saved:</h2>

      <ul>
      <% address_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
