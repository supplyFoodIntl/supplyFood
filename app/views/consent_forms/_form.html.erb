<%= form_with(model: consent_form, local: true) do |form| %>
  <% if consent_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consent_form.errors.count, "error") %> prohibited this consent_form from being saved:</h2>

      <ul>
      <% consent_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <textarea  cols= "80"  rows= "20" readonly>
            <%= @consent_form.consent_form_type.html_text %>
      </textarea>
  </div>



  <div class="field">
    <%= form.hidden_field :consent_form_type_id %>
  </div>

  <div class="field">
    <%= form.hidden_field :user_id %>
  </div>

  <div class="field">
      <%= form.label "I Agree" %>
    <%= form.check_box :is_signed %>
  </div>

  <div class="actions">
    <%= form.submit "Proceed" %>
  </div>

<% end %>
