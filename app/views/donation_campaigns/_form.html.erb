<%= form_with(model: donation_campaign, local: true) do |form| %>
  <% if donation_campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donation_campaign.errors.count, "error") %> prohibited this donation_campaign from being saved:</h2>

      <ul>
      <% donation_campaign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :address_id %>
    <%= form.text_field :address_id %>
  </div>

  <div class="field">
    <%= form.label :date_start %>
    <%= form.datetime_select :date_start %>
  </div>

  <div class="field">
    <%= form.label :date_end %>
    <%= form.datetime_select :date_end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
