<%= form_with(model: good_type, local: true,class: "form-horizontal") do |form| %>
  <% if good_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(good_type.errors.count, "error") %> prohibited this good_type from being saved:</h2>

      <ul>
      <% good_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="form-group field">  
        <div class="control-label col-sm-4">
            <%= form.label :name %>
        </div>
        <div class="col-sm-8">
            <%= form.text_field :name, class:"form-control",placeholder:"Name of the good type", autofocus:true %>
        </div>
    </div>

    <div class="form-group field">
        <div class="control-label col-sm-4">
            <%= form.label :good_type_id %>
        </div>
        <div class="col-sm-8">
            <%= collection_select( :good_type,:good_type_id,  GoodType.all, :id, :name,class:"form-control" ) %>
        </div>
    </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
