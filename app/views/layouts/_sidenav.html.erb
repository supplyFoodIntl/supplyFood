    <!-- Side navigation -->
    <div class="sidenav">
        <%= link_to image_tag((asset_path 'home200.png') ,  width: '200'), home_path  %>
        <%= link_to 'Donate!', new_donation_url %>
        <%= link_to 'Volunteer!', new_volunteer_url %>
        <%= link_to 'Supply!', new_supplier_url %>
        <%= link_to 'Find Food!', donation_campaigns_url %>
        <%= link_to 'Call for Help!', point_hunger_url %>
		<div class="dropdown">
				<button class="btn btn-xs btn-primary dropdown-toggle" 
							type="button" id="dropdownMenuButton" data-toggle="dropdown" 
							aria-haspopup="true" aria-expanded="false">
					Management
				</button>
				<div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
					<ul class="list-group">
						<%= link_to 'Main Tables', main_tables_management_url, class: "list-group-item" %>
						<%= link_to 'Manager', manager_management_url, class: "list-group-item" %>
						<%= link_to 'Volunteer', volunteer_management_url, class: "list-group-item" %>
						<%= link_to 'Courier', supplier_management_url, class: "list-group-item" %>
						<%= link_to 'Donee', donee_management_url, class: "list-group-item" %>
					</ul>
				</div>
			</div>
        
        <hr>
        <%= link_to 'Food Handling', foodhandling_url %>
        <%= link_to 'The value of a donation', valueofdonation_url %>
        <%= link_to 'Legal', legal_url %>
        <%= link_to 'Services', services_url %>
        <%= link_to 'News', news_url %>
        <div id="navBarStats">
            <strong>Stats</strong><br/>
            <span id="donations"></span>
            <span id="donatedTo"></span>
        </div>
        <script>
            function getData() { //this will read file and send information to other function
                var xmlhttp;

                if (window.XMLHttpRequest) {
                    xmlhttp = new XMLHttpRequest();
                } else {
                    xmlhttp = new ActiveXObject("Microsoft.XMLHTTP");
                }

                xmlhttp.onreadystatechange = function() {
                    if (xmlhttp.readyState == 4) {
                        var lines = xmlhttp.responseText; //*here we get all lines from text file*

                        intoArray(lines); //here we call function with parameter "lines*"                   
                    }
                }

                xmlhttp.open("GET", "/resources/stats/stats.txt", true);
                xmlhttp.send();
            }

            function intoArray(lines) {
                // splitting all text data into array "\n" is splitting data from each new line
                //and saving each new line as each element*

                var lineArr = lines.split('\n');

                document.getElementById("donations").innerHTML = "<span>Donations : " + lineArr[1] + "</span><br/>";
                document.getElementById("donatedTo").innerHTML = "<span>People Supplied :" + lineArr[3] + "</span><br/>";
            }

        </script>

        <script>
            getData();

        </script>

    </div>

