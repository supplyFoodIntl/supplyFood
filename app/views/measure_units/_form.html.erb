<%= form_with(model: measure_unit, local: true, class: "form-horizontal") do |form| %>
  <% if measure_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measure_unit.errors.count, "error") %> prohibited this measure_unit from being saved:</h2>

      <ul>
      <% measure_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="form-group field">  
        <div class="control-label col-sm-4">
            <%= form.label :name %>
        </div>
        <div class="col-sm-8">
            <%= form.text_field :name, class:"form-control",placeholder:"Measure name", autofocus:true %>
        </div>
    </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
