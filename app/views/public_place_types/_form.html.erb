<%= form_with(model: public_place_type, local: true) do |form| %>
  <% if public_place_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_place_type.errors.count, "error") %> prohibited this public_place_type from being saved:</h2>

      <ul>
      <% public_place_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
